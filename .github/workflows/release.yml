name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Versión (ej: v0.2.0)'
        required: true
        type: string

env:
  CARGO_TERM_COLOR: always

jobs:
  create-release:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v5

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: x86_64-pc-windows-msvc

    - name: Cache cargo dependencies
      uses: Swatinem/rust-cache@v2

    - name: Build release binary
      run: cargo build --release --verbose

    - name: Create release directory structure
      shell: pwsh
      run: |
        New-Item -ItemType Directory -Force -Path release\bin
        New-Item -ItemType Directory -Force -Path release\ejemplos
        New-Item -ItemType Directory -Force -Path release\skills
        New-Item -ItemType Directory -Force -Path release\agents

    - name: Copy artifacts to release directory
      shell: pwsh
      run: |
        # Copy binary
        Copy-Item target\release\mejia.exe release\bin\

        # Copy examples
        Copy-Item ejemplos\*.fc release\ejemplos\

        # Copy docs
        Copy-Item README.md, LICENSE, INSTALL.md, GUIA.md, REFERENCIA.md, ERRORES.md, CHANGELOG.md release\
        Copy-Item GUIA release\GUIA -Recurse

        # Copy skills & agents
        Copy-Item skills\* release\skills\ -Recurse
        Copy-Item agents\* release\agents\ -Recurse

        # Copy installers
        Copy-Item install.ps1 release\
        Copy-Item bundle_dlls.ps1 release\
        
    - name: Create release ZIP
      shell: pwsh
      run: |
        $ver = "${{ github.event.inputs.version }}"
        if (-not $ver) { $ver = "${{ github.ref_name }}" }
        Compress-Archive -Path release\* -DestinationPath "mejia-$ver.zip"

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        files: mejia-*.zip
        generate_release_notes: true
        body: |
          ## Mejia ${{ github.event.inputs.version || github.ref_name }}

          Lenguaje de programación de sistemas de alto rendimiento con sintaxis en español sobre Cranelift.

          ### Instalación
          1. Descarga `mejia-${{ github.event.inputs.version || github.ref_name }}.zip`
          2. Extrae en cualquier carpeta
          3. Ejecuta `install.ps1` (PowerShell) — menú interactivo con opciones
          4. Abre una terminal nueva y escribe `mejia version`

          ### Qué instala install.ps1 (opcional, eliges cada uno):
          - mejia.exe en PATH (siempre)
          - Extensión VS Code (syntax + LSP + tema Mejia Dorado)
          - Agent + Skill para OpenCode
          - Agent + Skill para Claude Code
          - Cursor: usa la misma extensión VS Code

          ### Requisitos
          - Windows 10 o superior (x86_64)
          - No requiere Rust ni VS Build Tools
