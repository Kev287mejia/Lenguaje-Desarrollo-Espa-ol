// Ejemplo de bucle para (for-each) en mejia
// Itera sobre arrays

función principal() -> Entero32 {
    los nums: [Entero32; 5] = [10, 20, 30, 40, 50];
    
    el suma: Entero32 = 0;
    para num en nums {
        suma = suma + num;
    }
    
    retornar suma;
}

